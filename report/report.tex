\documentclass[a4paper, 11pt]{article}          % Paper and font size
\usepackage[ngerman,english]{babel}               % Language setting

% Set margins
\usepackage[top=2.5cm,bottom=2.5cm,left=2cm,right=2cm,marginparwidth=1.75cm]{geometry}

% Load useful packages
\usepackage{graphicx}                           % Graphics
\graphicspath{{figures/}}                       % Look for images in report/figures/
\usepackage{amsmath}                            % Math
\usepackage{xcolor}                             % Link color
\definecolor{custom-blue}{RGB}{0,99,166}
\usepackage{hyperref}
\hypersetup{colorlinks=true, allcolors=custom-blue}
\usepackage[default]{sourcesanspro}                % Load font
\usepackage[T1]{fontenc}                        % Special characters
\usepackage{fancyhdr}                           % Load header, footer package

\usepackage{lastpage}                           % Footer note
\usepackage{lipsum}                             % For dummy text

\pagestyle{myheadings}                          % Own header
\pagestyle{fancy}                               % Own style
\fancyhf{}                                      % Clear header, footer

\setlength{\headheight}{30pt}                   % Set header hight
\renewcommand{\headrulewidth}{0.5pt}            % Top line
\renewcommand{\footrulewidth}{0.5pt}            % Bottom line

\fancyhead[L]{\includegraphics[width=3cm]{univienna-logo.eps}} % Header left
\fancyhead[C]{Head center}                                 % Header center
\fancyhead[R]{University of Vienna}             % Header right
\fancyfoot[L]{Footer left}         % Footer left
\fancyfoot[C]{Footer center}                                 % Footer center
\fancyfoot[R]{\thepage/\pageref{LastPage}}      % Footer right



%% CONTACT: zid.univie.ac.at
%% KONTAKT: zid.univie.ac.at


%%% Begin document %%%
\begin{document}

    \begin{figure}                                  % Include Logo
        \flushleft
        \includegraphics[width=0.4\textwidth]{univienna-logo.eps}\label{fig:logo}
    \end{figure}

% Set title, author, date
    \title{Diagrammatic Quantum Monte Carlo Project}
    \author{Valentin Mayer | a12213346@unet.univie.ac.at}
    \date{\today{}, Vienna}

    \maketitle                                      % Include contents
    \tableofcontents

    \newpage
    \section{Task 2: Estimating $\pi$}                              % Example sections, subsections

    We can estimate $\pi$ using multiple different methods. The two that are used here are the area method and the intergration method.

    \subsection{Convergence}

    \begin{figure}[h]
        \centering
        \includegraphics[width=0.8\textwidth]{task2/pi_convergence}
        \caption{Convergence of the area and integration methods to $\pi$.}
        \label{fig:task2-convergence}
    \end{figure}

    \begin{figure}[h]
        \centering
        \includegraphics[width=0.8\textwidth]{task2/pi_distribution}
        \caption{Distribution of the area and integration methods to $\pi$.}
        \label{fig:task2-distribution}

    \end{figure}

    Gaussian/Normal distribution (right?) The second one is better/ more effective, smaller variance.

    Mean value is pi if N goes to infinity -> unbiased.

    Could do some importance sampling but no huge gains expected from that

    \section{Task 3}\label{sec:task-3}

    The goal of this exercise is to approximate following integrals using MC intergration:

    In Monte Carlo integration we approximate the integral of a function as the expectation value over a certain interval. For the interval [0,1]:

    \begin{equation}
        \label{eq:int}
        Q_n(f) = <f>_n = \frac{1}{n}*\sum_{j=1}^{n} f(x_j) \approx \int_{0}^{1} f(x)= I_d(f)
    \end{equation}

    with

    \begin{equation*}
        \lim_{n->\infty} Q_n = I_d
    \end{equation*}

    There are multiple ways to calculate the error of the approximation that will be discussed in more detail later. For now we will look at the simplest way. As each random draw $x_k$ is random they are uncorrelated and we can write use the variance of the mean as an error approximation:

    \begin{equation*}
        \sigma_{\bar{f}}^2 = \frac{\sigma_f^2}{N}
    \end{equation*}

    So the error is directly dependent of the variance of our function f. We can reduce this variance by using importance sampling. What this means is splitting f(x) in the following way

    \begin{equation}
        \label{eq:importance_splitting}
        f(x) = g(x) \times p(x)
    \end{equation}

    where p(x) is a probability density, meaning it is normalized in the interval of interest.

    Plugging \eqref{eq:importance_splitting} into \eqref{eq:int} we get the estimation of our integral $Q_n(f)$ as

    \begin{equation}
        \label{eq:mc_integral}
        Q_n(f)=Q_n(g)=\sum_{j=1}^N g(x_j)
    \end{equation}

    where $x_j$ is distributed according to p(x).
    So by choosing p(x) in a way that reduces the variance the estimation can be improved significantly.

    Sometimes it is possible to obtain a set of values $x_k$, distributed according to a p(x) by using CDF inversion, which is demonstrated on the following example: We are trying to generate random $\tau$ distributed according to $\exp(-\tau)$.
    The results obtained by using CDF inversion can be observed in figure \ref{fig:task3-cdf_histogram}.

    \begin{figure}[h]
        \centering
        \includegraphics[width=0.8\textwidth]{task3/cdf_histogram}
        \caption{Histogram of 100000 random generated values distributed according to p(x), and plot of exact function Q($\tau$) for comparison}
        \label{fig:task3-cdf_histogram}

    \end{figure}

    The histogram is normalized by dividing the counts in each bin with the total number of points and the bin width. $Q(\tau)$ is normalized by solving the equation:

    \begin{equation*}
        \frac{\int_0^5 \exp(-\tau) d\tau = 1}{NORM}
    \end{equation*}

    for NORM and using this constant as a normalization factor for Q. The normalization factor therefore is $1-\exp(-5)$.

    Now that we have $p(x)$ and $f(x)$ (the function to integrate) we can easily obtain $g(x)$ through \eqref{eq:importance_splitting} and evaluate the integrals of the task,

    \begin{equation*}
        I_1 = \int_0^5 \tau \, Q(\tau) \, d\tau, \qquad
        I_2 = \int_0^5 \tau^2 \, Q(\tau) \, d\tau, \qquad
        Q(\tau) = \exp(-\tau),
    \end{equation*}

    using Monte Carlo integration with $N=10^6$ samples drawn via CDF inversion. The error on each estimate is the standard deviation of the mean, $\sigma_{\bar{f}} = \sigma_f/\sqrt{N}$, computed from the sample variance of the integrand. This yields

    \begin{align*}
        I_1 &= 0.9599 \pm 0.0009, \\
        I_2 &= 1.7521 \pm 0.0032.
    \end{align*}

    Both results agree with the analytic values $I_1 = 1-6e^{-5} \approx 0.9596$ and $I_2 = 2-37e^{-5} \approx 1.7507$ within one to two standard deviations, as expected for an unbiased estimator.


    \section{Task 4}

    I am optimizing the delta to minimize the autocorrelation time using data blocking.
    I evaluate the integrated autocorrelation time at a course grained log grid, to have robust search with approximate minimum.
    This will leave me with the smallest possible variance for the steps I use.
    Theoretically the autocorrelation changes depending on which integral I solve, but for now this rough approx is enough.

    Acceptance probability of 1 means either uniform distribution or staying on the same point
%%% End document %%%
\end{document}